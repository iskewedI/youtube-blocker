export enum TimeRange {
  From,
  To,
}

export enum CriteriaListType {
  Allow = 1,
  Block,
}

export enum Screens {
  Profiles = 1,
  Timer,
  Settings,
  CriteriaPanel,
}

export enum Categories {
  Tags = 1,
  Titles,
  Channels,
}
